"""Local Textbook Chat application package.

The application package intentionally sits beside, rather than inside, the
research package.  It may consume frozen baseline interfaces but must never
mutate the frozen research configuration or claim benchmark validation for a
newly uploaded textbook.
"""

__version__ = "0.1.0"
